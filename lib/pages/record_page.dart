import 'package:flutter/material.dart';

class RecordPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('السجل')),
      body: Center(child: Text('اهلا هذا سجلك الفارغ ')),
    );
  }
}
